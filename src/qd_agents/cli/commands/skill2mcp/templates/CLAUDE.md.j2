# CLAUDE.md - {{ skill_name }} MCP Server

This MCP server provides access to the {{ skill_name }} skill via the Model Context Protocol.

## Architecture

- `scripts/main.py` - Main MCP server implementation (Python)
- `pyproject.toml` - Python project configuration and dependencies
- `test/validate.py` - Python validation script to verify server functionality

## Development

This is a Python MCP server using uv for package management.

```bash
# Install uv (if not already installed)
pip install uv

# Install dependencies
uv pip install -e .

# Run the server
uv run {{ package_name }} -m scripts.main

# Or run directly
python -m scripts.main
```

## Configuration

The server reads configuration from the original skill directory.

## Package Management

This project uses uv for fast, reliable Python package management.

## Notes

This is an auto-generated MCP server wrapper for the {{ skill_name }} skill.