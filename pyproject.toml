[project]
name = "qd-agents"
version = "0.1.0"
description = "意图驱动的上下文隔离多Agent系统"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.0.0",
    "typer>=0.12.0",
    "rich>=13.0.0",
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = true

[dependency-groups]
dev = [
    "mypy>=1.0.0",
    "pytest>=8.0.0",
]

[project.scripts]
qd-agents = "qd_agents.cli:main"
